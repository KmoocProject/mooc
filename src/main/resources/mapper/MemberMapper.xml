<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.fullstack7.mooc.mapper.MemberMapper">
    <select id="selectMemberById" resultType="net.fullstack7.mooc.domain.Member">
        SELECT *
        FROM member
        WHERE memberId = #{memberId}
        AND enabled != 'N'
    </select>

    <insert id="insertMember">
        INSERT INTO member(memberId, password, userName, email, gender, memberType, status, credit, createdAt
        ) values (
                  #{memberId}, #{password}, #{userName}, #{email}, #{gender}, #{memberType}, #{status}, #{credit}, #{createdAt}
                  )
    </insert>

</mapper>